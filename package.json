{
  "name": "talent-graph",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Algorithm core for the Talent Graph: Referral Signal (V0) and Relative Capability (V1). No UI, no DB, zero runtime dependencies.",
  "module": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "demo": "bun run scripts/demo.ts",
    "club:web": "doppler run -- bun --cwd apps/club dev",
    "club:build": "doppler run -- bun --cwd apps/club build",
    "drift": "bun run scripts/drift.ts",
    "club:convex": "doppler run -- bun --cwd apps/club convex:dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/bun": "^1.4.0",
    "typescript": "^5.9.3"
  }
}
