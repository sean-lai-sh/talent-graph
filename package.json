{
  "name": "talent-graph",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Algorithm core for the Talent Graph: Referral Signal (V0) and Relative Capability (V1). No UI, no DB, zero runtime dependencies.",
  "module": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome format --write .",
    "demo": "bun run scripts/demo.ts",
    "demo:ui": "bun run scripts/demo-ui.ts",
    "drift": "bun run scripts/drift.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@types/bun": "^1.4.0",
    "typescript": "^5.9.3"
  }
}
