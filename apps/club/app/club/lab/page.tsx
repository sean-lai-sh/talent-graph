import { ClubShell } from "../ClubShell";

/**
 * Lab surface for the signed-in club door. Same auth gate as `/club`.
 * Compare, judge clock, and graph stay reachable here.
 */
export default function ClubLabDoor() {
  return <ClubShell />;
}
